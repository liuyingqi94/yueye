语法
Html是以.html或.htm位扩展名的文件
所有内容都以<标签>的形式编写
标签都有<开始></结束>，部分独立标签无结束，会在标签最后加/代表结束<br/>

基本结构
<html><head></head><body></body></html>

<标签 属性=“值” 属性=“值” 属性=“值” ...>文本</标签>

标签可以任意嵌套使用，但要注意开始和结束的顺序（先开始后结束，后开始先结束）

id,class,name仅用于标记元素，无实际意义